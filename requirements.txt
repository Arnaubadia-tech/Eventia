# First install Cython explicitly
Cython==3.0.10

# Then force binary wheels for known problematic packages
numpy==1.26.4 --only-binary=:all:
pandas==2.2.2 --only-binary=:all:
scipy==1.13.1 --only-binary=:all:

# Other regular dependencies
flask==3.0.2
requests==2.31.0
